using System;
using UnityEngine;

[CreateAssetMenu(fileName = "LevelData", menuName = "Duck/LevelData"), Serializable]
public class LevelSO : ScriptableObject
{
    int width;
    int height;


    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
