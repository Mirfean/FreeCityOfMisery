using UnityEngine;

public class EnemyHand : MonoBehaviour
{
    [SerializeField] GamePiece[] hand;

    public GamePiece[] Hand { get => hand; set => hand = value; }

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
